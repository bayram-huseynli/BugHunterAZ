spring:
  datasource:
    url: jdbc:mysql://localhost:1009/ANADEARMAS
    username: root
    password: password
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update

  mail:
    host: smtp.example.com
    port: 587
    username: ruafyseyidov@gmail.com
    password: ausb enca syiw biyi
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

jwt:
  secret: ${jwt.secret}
  expirationMs: 3600000
